(ns beholder.services.k8s-service-doc)

(defn- trigger-inexing [since]
   ;; - get all recently updated services
   ;; - for each service get OpenaAPI doc and save it
   ;; - for each service get AsyncAPI doc and save it
   ;; - mark
   ;;
  )
